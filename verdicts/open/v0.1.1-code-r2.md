## [2026-09-04] — CODE review

Round 2 for v0.1.1. One prior verdict in `verdicts/open/`.

**Prior concern, status:**
- r1 blocker (untracked `reference/` files in a public repo, one `git add` from permanent publication): **fixed properly, not worked around.** A new `.gitignore` at the repo root ignores `reference/*` with a `!reference/.gitkeep` exception, and `reference/.gitkeep` now exists so the directory survives. Both offending files were untracked at session start, so ignoring them is effective — they were never in the index or history. The reference material stays on disk for its intended local use rather than being deleted, which is the right shape. `reference/*` also swallows any future subdirectory under it.
- r1 non-blocker 3 (`dir="rtl"`) was implemented rather than backlogged. Harmless here; noted below only because CLAUDE.md says non-blockers don't get fixed mid-cycle.
- r1 non-blockers 1, 2, 4 remain open and correctly untouched.

Fast path claim: still correct. The diff against the shipped `index.html` is three things — the Hebrew line composed from `heDateParts.d/m/y` instead of `data.hebrew`, `dir="rtl"` on `#clock-hebrew`, and the new `.gitignore`. Same endpoint, same params, no write, no credential, no PII, no change to who the system reaches, no new capability.

Code: the guard checks all three parts before rendering; failure or a missing part leaves `lastHebrewFetchDateKey` unset so the minute tick retries, and the last-good value stays up. `dir="rtl"` puts the day token rightmost, which is the conventional order. Nothing else in the file moved.

Non-blockers:
- The comment on the `heDateParts` choice still implies that field is free of nekudos. Per Hebcal's documented response `heDateParts.m` carries them (`אֱלוּל`); what `heDateParts` reliably drops is the `ב` prefix and the fixed ordering. The comment as written could mislead the next reader. Only the real panel settles what actually renders.
- Requiring three fields instead of one still gives more ways to sit blank with no indicator (r1 item 1, unchanged).
- I cannot verify the git index state from here, so the `.gitignore` is judged as a file, not as a confirmed-clean staging area. Worth one `git status` glance before the Phase 4 commit to confirm neither `reference/Code.gs` nor `reference/index.html` appears.

Security review: PASSED. No secret, token, or internal URL added. External Hebcal fields go through `textContent`, no `innerHTML`, no interpolation into markup or into a URL beyond `searchParams.set`. Fetch still bounded by abort. The Drive path remains gated on flag AND non-empty key, failing closed. The `.gitignore` is a net reduction in exposure: it closes the one credential/PII publication path r1 found.

Backlog items noted:
1. Correct or drop the nekudos claim in the `heDateParts` comment once the rendered string is seen on the panel.
2. Carry forward r1 items 1, 2 and 4 (degraded state for the Hebrew line; Hebrew script vs. transliterated English for consistency with the sibling screen; on-panel confirmation of nekudos, glyph coverage and width at 3vh).

VERDICT: SHIP

Summary:
1. The r1 blocker is genuinely fixed — `.gitignore` excludes `reference/*` with a `.gitkeep` exception, and those files were untracked, so nothing credential- or PII-bearing can reach the public push.
2. The Hebrew-date formatting change is small, safely rendered as text, correctly guarded, and legitimately fast-path.
3. Everything remaining is backlog, and per CLAUDE.md the rendered Hebrew string is unverified until it is on the mounted portrait screen.
