## [2026-09-04] — SPEC review

No version was stated in the call. Taken as v0.1.0 from `verdicts/open/v0.1.0-spec-r1.md`, the prior verdict for this same phase — not from `SPEC.md`. Round 2, resubmission.

**Prior concern status — r1 blocker (Drive API key had no defined safe home): RESOLVED.** The spec no longer contradicts itself. It now states plainly that key *creation* is the external step, that the key then lives in the committed config block, and that HTTP-referrer restriction plus Drive-API scoping — not secrecy — are what make it safe. That is one of the two resolutions I named, it is internally consistent, and no key exists this cycle so nothing exposed ships. Fixed, not worked around.

Four questions on the current spec: right problem, yes. Approach unsafe, no. Scope, right for one cycle. One missing case worth naming below, but it does not change the shape.

Non-blockers:
- The resolution above rests on a Phase 0 confirmation from the user that a referrer-restricted key is fine to commit. I cannot see Phase 0, and `CLAUDE.md` carries a standing blanket rule ("never commit a secret, token, API key") that this deliberately carves an exception to. The decision is logged and reversible, so it is not a spec blocker — but the exception should get an explicit yes at Phase 3.5 before any real key is ever pasted in, since that act is the irreversible half.
- **The fixed 10-minute deck reload restarts the deck at slide one.** If the deck's own loop is longer than the reload interval, the tail of the deck never displays — the page works, and the content silently doesn't. The spec makes the interval configurable, so the fix is one number, but nothing in it ties that number to the deck's loop length. Product decision on what the interval should be; worth a sentence in the spec or a build-time check against the actual deck.
- `BACKLOG.md` is still empty — none of the four items from r1's verdict were appended. Process, not spec.
- Midnight rollover of the secular and Hebrew dates is still unstated (r1 non-blocker, unchanged, still fine to leave to the build).
- Clock watchdog reloads the whole page, which discards the last-good Hebrew date; if Hebcal is unreachable at that moment the strip has no value to fall back to. Minor.
- Hebcal retry/backoff still unspecified (r1 non-blocker, unchanged).
- Viewport readout still default-on and publicly visible (r1 non-blocker, intentional).

Behavior-only, no code blocks or file citations. Length and scope are right.

Security review: not applicable — spec review.

Backlog items noted (r1's four still not appended, plus):
5. Set the fixed deck-reload interval to at least one full deck loop, or reconsider timer-based reload for long decks.
6. Hebrew/secular date rollover at midnight.
7. Post-reload gap: no last-good Hebrew date available immediately after a watchdog page reload.

VERDICT: APPROVED TO BUILD

Summary:
1. The r1 blocker is genuinely resolved — the key's home and its restrictions are now stated and consistent, and no key ships this cycle.
2. Nothing new rises to blocker level; the spec describes behavior and is correctly scoped.
3. Two things for the user at Phase 3.5: explicit sign-off on the committed-key exception to `CLAUDE.md`, and the deck-reload interval versus deck loop length.
