## [2026-09-04 — no shell access in this review, time approximate] — CODE review

`verdicts/open/` held only `.gitkeep`, so this is round 1 with no prior concerns to check.

`proposed-fix/index.html` differs from live `index.html` in exactly two places: the `#clock-time` initial placeholder (`--:--:-- --` → `--:-- --`) and the `timeFormatter` options (`second: "2-digit"` dropped). Everything else — deck config and crop, rotation hook, Drive polling block, watchdog thresholds, Hebcal path, `viewportDebugEnabled: false`, `rotationDegrees: 0` — is byte-identical.

Fast-path claim holds: a display-format change to an existing element. No data written or deleted, no person moved, no money or billed API, no credentials or PII, no change to who the system reaches, no new capability. Correct path.

Behavior check: `updateClock` still runs on the 1s interval and still advances `lastTickAt` on every successful render, so the 15s clock-stale watchdog is unaffected. Keeping the 1s interval (rather than aligning to the minute) means the minute rollover appears within a second — correct, not a leftover. `timeEl.textContent` is now reassigned to an identical string ~59 times per minute; harmless, but it is the one write in `updateClock` not guarded by an inequality check the way `dateEl` is.

Non-blocker: the seconds readout was the only human-visible liveness signal on the panel. The JS-level watchdog still catches `updateClock` dying, but a frozen renderer stops the watchdog's own timer too, and at minute resolution a stall is now invisible to a passerby for up to a minute. This is a display/product tradeoff, not a defect — flagging it for the user, not asking for a change.

Non-blocker: `timeEl` could take the same `if (textContent !== ...)` guard `dateEl` already has, saving a needless DOM write per second on a 24/7 display. Cosmetic.

Non-blocker: per the project header, this is unverified until seen on the actual Cast-driven panel — the shorter string reduces width, so no overflow risk at `9vh`, but that's reasoning, not verification.

Security review: PASSED. No new external input, no new interpolation, no auth or authorization surface, no credential handling. `driveApiKey` still empty and `changeDetectionEnabled` still false. Removing seconds from a public hallway clock exposes nothing and conceals nothing sensitive.

Backlog items noted:
1. Guard the `#clock-time` write with an equality check, matching how `#clock-date` is handled.
2. Consider a subtler liveness cue on the strip (e.g. a blinking colon or separator) now that seconds no longer show a stall to the naked eye.

VERDICT: SHIP

Summary:
1. Two-line change removing seconds from the clock display and shortening the matching placeholder; nothing else differs from what's live.
2. Fast path correctly chosen; security review passed; watchdog and all recovery paths unaffected; no blockers.
3. Non-blockers: loss of the only human-visible liveness cue (product tradeoff, user's call), an unguarded per-second DOM write, and no on-panel verification yet.
