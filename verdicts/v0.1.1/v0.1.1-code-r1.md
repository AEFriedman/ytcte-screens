## [2026-09-04] — CODE review

Round 1 for v0.1.1. `verdicts/open/` is empty (v0.1.0 archived), so no prior concerns to re-check.

Fast path claim: correct for the diff. The only change to `proposed-fix/index.html` vs. the shipped `index.html` is how the Hebrew date line is composed — `data.hebrew` replaced by `heDateParts.d/m/y` joined with spaces. Same endpoint, same query params, no write, no credential, no new capability, no change to who the system reaches. Display formatting only.

The code change itself is clean: `textContent`, no `innerHTML`, no new interpolation, guard before render, failure still holds last-good and retries on the next minute tick.

**Blocker (implementation/repo safety, not the diff): the untracked `reference/` files must be excluded from git before anything in this cycle is committed.** `reference/Code.gs` contains a Twilio Account SID, two staff WhatsApp phone numbers, and Drive folder/doc IDs, and carries its own line "WARNING: Never commit this file to the public GitHub repo." `reference/index.html` contains a live Apps Script `/exec` URL deployed as "Anyone, even anonymous," which serves internal doc and photo content to whoever holds it. There is no `.gitignore` in this repo, the repo is public, and Phase 4 is apply → commit → push, where the push publishes. A staged `git add -A` at ship makes that permanent in history. This is cheap to fix and irreversible if missed, which is why it blocks rather than going to the backlog.

Non-blockers:
- The new comment says `heDateParts` avoids nekudos. Per Hebcal's documented response, `heDateParts.m` carries nekudos (e.g. `אֱלוּל`); what `heDateParts` actually drops is the `ב` prefix and lets you control ordering. If nekudos-free was the goal, this likely does not achieve it — only the real screen settles it.
- The previously shipped sibling screen (`reference/Code.gs`) renders this as transliterated English (`hd hm hy` → "22 Elul 5786"). Switching this screen to Hebrew script is a product/consistency call, not mine.
- Success now requires three fields instead of one, so there are marginally more ways to sit blank forever with no visible indicator. The Hebrew line still has no degraded state, unlike the deck poll.
- `#clock-hebrew` has no `dir` attribute; correct visual order relies on implicit bidi over three RTL tokens. `dir="rtl"` would make it deterministic.

Security review: FAILED — not on the diff (that part passes: no secret, no unsafe interpolation, external data rendered as text, fetch still bounded by abort), but on the ship as a whole. See the blocker: credential-adjacent and PII-bearing untracked files sit in the working tree of a public repo with no `.gitignore`, one commit away from permanent publication.

Backlog items noted:
1. Give the Hebrew date line a visible degraded state when the fetch has been failing across a rollover, matching the deck poll's indicator.
2. Decide whether the Hebrew date shows Hebrew script or transliterated English, given the existing screen uses English.
3. Add `dir="rtl"` to the Hebrew date element rather than relying on implicit bidi.
4. Confirm the rendered Hebrew string on the actual panel — nekudos, glyph coverage in the chosen font stack, and width at 3vh.

VERDICT: DO NOT SHIP

Summary:
1. The Hebrew-date formatting change is correct, safely rendered, and legitimately fast-path; no blocker in the diff.
2. Blocker is the repo state around it: `reference/Code.gs` and `reference/index.html` are untracked, un-ignored, contain a Twilio SID, staff phone numbers, internal Drive IDs and an anonymous-access Apps Script endpoint, and this cycle's push publishes to a public repo. Exclude them before committing.
3. Everything else is backlog, and the rendered Hebrew string is unverified until it is on the mounted screen.
