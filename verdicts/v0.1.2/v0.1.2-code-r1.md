## [2026-09-04 07:30 — approx., no shell access in this review] — CODE review

`verdicts/open/` held only `.gitkeep`, so this is round 1 with no prior concerns to check.

`proposed-fix/index.html` differs from the live `index.html` in exactly one character position: `CONFIG.viewportDebugEnabled` goes `true` → `false`. Nothing else in the file changed — deck config, reload/watchdog timings, Hebcal path, rotation hook, Drive polling block are byte-identical. `rotationDegrees` stays `0`.

Fast-path claim holds: a display-only debug flag. No data written or deleted, no person moved, no money or billed API, no credentials or PII, no change to who the system reaches, no new capability. Correct path.

Behavior with the flag off: `renderViewportDebug()` returns early, and `#viewport-debug` is `display: none` in the stylesheet, so the overlay never appears. The `resize` listener stays registered and is a no-op. Nothing else reads the flag. No silent-failure surface introduced — the degraded indicator and the clock watchdog are untouched.

Non-blocker (backlog): the debug readout was the only way to learn the real Cast's viewport dimensions, and turning it off records nothing. Whoever next questions the layout has to flip the flag back on and re-measure. Worth writing the measured dimensions into `SESSION-LOG.md` or a comment beside `viewportDebugEnabled`.

Non-blocker / for the user, not a code issue: this flag going off implies the orientation question from v0.1.0 was answered on the actual screen and came back portrait-native (`rotationDegrees` still `0`). I can't verify that from the code. If it wasn't actually confirmed on the panel, flipping this back is a one-character revert.

Security review: PASSED. No new external input, no interpolation, no auth or authorization surface, no credential handling, `driveApiKey` still empty and change detection still gated off. The overlay it disables only ever printed `window.innerWidth x window.innerHeight`, so removing it neither exposes nor conceals anything sensitive; net effect on a public hallway screen is marginally less on-screen noise.

Backlog items noted:
1. Record the measured real-panel viewport dimensions somewhere durable, so re-measuring doesn't require re-enabling the debug flag.

VERDICT: SHIP

Summary:
1. Single-value config change (`viewportDebugEnabled: true → false`); rest of the file is identical to what's live.
2. Fast path correctly chosen; security review passed; no blockers.
3. Two non-blockers: nothing records the measured viewport dimensions, and the code can't confirm the orientation check that justifies turning the readout off.
